node_modules
dist
dist-ssr
*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# graphify knowledge-graph output (regenerated by /graphify)
graphify-out/

# editor / OS
.DS_Store
.idea
.claude/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln

# generated by tools/logo/build.py, consumed by png.py
tools/logo/assets.json

# the tools' python venvs (each tool's README says how to make one)
tools/*/.venv/
# the font subset tools/pdffont downloads; only the outlines it extracts are committed
tools/pdffont/subset.woff2

# working notes, kept out of the repo on purpose: they are a diary of what was tried and
# measured in one session, nothing verifies them, and they go stale without ever looking wrong.
# What a contributor must not break lives in CONTRIBUTING.md and in the module header comments.
CLAUDE.md
docs/design-notes.md
