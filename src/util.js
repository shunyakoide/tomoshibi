/**
 * ============================================================================
 * 共通ユーティリティ (UTIL)
 * ============================================================================
 * UI 側(HarigataStudio / SectionEditor)で共有する小さな純粋関数。
 * ============================================================================
 */

// 値を [lo, hi] に収める。
export const clamp = (lo, hi, v) => Math.max(lo, Math.min(hi, v));
